
#include <stdio.h>
#include <stdlib.h>

//#define N 100

double polinomio[] = 7, 3, 2;

/*double buscar(){

}

double pedir(){
    double x;

    prinf("Escribe el valor de x: ");
    scanf(" %lf", &x);

    return x;
}

double elevado(double x, int i, double num){

    double pot = 1;

    for(int a = 1; a < i; a++)
        pot = pot * x;
    return num * pot
}
*/
int main(int argc, char *argv[]){

    /*double x, num, i;

    x = pedir();

    printf("%.2%lf", elevado(x, i, num));
    */
    
    for(int j=0; j<=max_cof; j++, potencia*=x)
        resultado += coeficiente[j] * potencia;

    return EXIT_SUCCESS;


}
